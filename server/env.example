# Configurazione Server
PORT=5000
NODE_ENV=development

# Configurazione CORS (separare con virgole per più origini)
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000

# Configurazione Logging
LOG_LEVEL=info
